#include <bits/stdc++.h>

using namespace std;

void TestCase(){
    int n, m; cin >> n >> m;
    string s; cin >> s;
    string t = "";
    // for(int i = 0; i < n; i++){
    //     t += "?";
    // }
    cout << 1 << '\n';
}

int main() {
    ios_base::sync_with_stdio(true);
    cin.tie(); cout.tie();
    int T = 1; cin >> T;
    while(T--){
        TestCase();
    }
    return 0;
}